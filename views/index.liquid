<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <title>{{title}}</title>
        <meta name="description" content="">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <link rel="stylesheet" href="/stylesheets/style.css">
    </head>
    <body>
        {% comment %} This div will contain an image and contents {% endcomment %}
        <div>
            <img src="/images/blog.png" alt="Blog Image" style="width:320px;height:160px;" borderline = black>
            <ul>
                {% for txt in data%}
                    <ul>
                        {{txt.blog_id}}.
                        {{txt.blog_title}}
                        <form action="/update" method="GET">
                            <button>Update</button> 
                        </form>
                    </ul>
                {% endfor%}
            </ul>
        </div>

        <div id="grid-container">
        <div>
            <h2>Add items</h2>
            <form action="/write" method="GET">
                <div>
                    <button>New page to Writing</button>
                </div>
            </form>
        </div>
        <div>
            <h2>Delete Items</h2>
            <form action="/delete" method="POST">
                    <div>
                        <label for="blog">Delete item number: </label>
                        <input name="blog" id="blog"/>
                    </div>
                    <div>
                        <button>delete item from List</button>
                    </div>
            </form>
        </div>
        </div>
    <div>
        
    </div>
    </body>
</html>